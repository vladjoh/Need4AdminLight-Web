namespace Need4AdminLight.Web;

/// <summary>Need4Admin Light — minimal read-only web scanner (sibling to the PowerShell Need4Admin script).</summary>
public sealed class Need4AdminOptions
{
    public const string SectionName = "Need4AdminLight";

    public string ProductName { get; set; } = "Need4Admin Light";
}
